[user]
	name = Matthew Martz
	email = xiao_haozi@mutaku.com
[color]
    diff = auto
    status = auto
    branch = auto

[push]
    default = matching

[core]
	editor = vim

[merge]
    tool = mvim

[mergetool "mvim"]
    cmd=mvim -d -g $LOCAL $MERGED $REMOTE
    keepbackup=false
