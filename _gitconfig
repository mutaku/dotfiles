[user]
	name = Erik Steigler
	email = erik@lot18.com
[color]
    diff = auto
    status = auto
    branch = auto

[push]
    default = matching

[core]
	editor = ~/bin/vim

[merge]
    tool = mvim

[mergetool "mvim"]
    cmd=mvim -d -g $LOCAL $MERGED $REMOTE
    keepbackup=false
